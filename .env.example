# Supabase — javne vrednosti (bezbedne za klijent, štiti ih RLS)
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-ili-publishable-kljuc>

# Server-only tajne (NIKAD u klijent, samo Vercel env / lokalni .env.local)
# SUPABASE_SERVICE_ROLE_KEY=<service-role-kljuc>   # samo za migracije/admin skripte
